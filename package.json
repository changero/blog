{
  "name": "blog",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "husky": "^3.1.0",
    "vuepress": "^1.9.7"
  },
  "scripts": {
    "start": "npm run dev",
    "dev": "cross-env NODE_OPTIONS=--openssl-legacy-provider vuepress dev docs",
    "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider vuepress build docs",
    "build:surge": "cross-env BUILD_ENV=surge vuepress build docs"
  }
}
